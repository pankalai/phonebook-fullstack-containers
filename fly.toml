# fly.toml app configuration file generated for full-stack-web-development-backend on 2024-06-24T16:07:05+03:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'full-stack-web-development-backend'
primary_region = 'arn'

[build]

[env]
  PORT = "3000"

[processes]
  app = "node index.js"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[deploy]
  strategy = "canary"

[[http_service.checks]]
  method = "GET"
  path = "/health"
  interval = "5s"
  timeout = "10s"
  grace_period = "90s"
